import type { Config } from "prettier";

const config: Config = {
  tabWidth: 2,
  useTabs: false,
  semi: true,
  singleQuote: false,
  bracketSpacing: true,
  arrowParens: "always",
  trailingComma: "none",
  printWidth: 120
};

export default config;
